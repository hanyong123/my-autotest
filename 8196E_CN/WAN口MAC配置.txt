*** Settings ***
Suite Setup       打开浏览器
Suite Teardown    关闭浏览器
Resource          keyword.txt

*** Test Cases ***
try
    验证WAN口MAC    ${EMPTY}    The MAC Address is invalid. Please input another one!

*** Keywords ***
验证WAN口MAC
    [Arguments]    ${mac}    ${message}
    转到DHCP接入设置页面
    设置DHCP接入参数    mac=${mac}
    显示消息    ${message}

验证WAN口MAC最大输入长度
    [Arguments]    ${mac}    ${max_len}
    转到DHCP接入设置页面
    验证输入框最大输入长度    id=mac_addr    ${mac}    ${max_len}
