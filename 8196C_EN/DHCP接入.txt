*** Settings ***
Suite Setup       Run Keywords    打开浏览器    AND    转到DHCP接入设置页面
Suite Teardown    关闭浏览器
Resource          keyword.txt

*** Test Cases ***
try
    验证DHCP首选DNS    .168.1.10    Invalid IP Address! Please input another one.

try1
    验证DHCP备选DNS    .168.1.10    Invalid IP Address! Please input another one.

*** Keywords ***
验证DHCP首选DNS
    [Arguments]    ${dns}    ${message}
    设置DHCP接入参数    dns1=${dns}    dns2=8.8.8.8
    显示消息    ${message}

验证DHCP首选DNS最大输入长度
    [Arguments]    ${dns}    ${max_len}
    验证输入框最大输入长度    id=dns_a    ${dns}    {max_len}

验证DHCP备选DNS
    [Arguments]    ${dns}    ${message}
    设置DHCP接入参数    dns1=8.8.8.8    dns2=${dns}
    显示消息    ${message}

验证DHCP备选DNS最大输入长度
    [Arguments]    ${dns}    ${max_len}
    验证输入框最大输入长度    id=dns_b    ${dns}    {max_len}
